name: Build and Publish Pegasus Plus Image

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/pegasus-plus-image.yml"
      - "containers/pegasus-plus/**"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-and-publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract image metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository_owner }}/pegasus-plus
          tags: |
            type=raw,value=latest
            type=raw,value=v0.1.0
            type=sha,format=short,prefix=sha-
          labels: |
            org.opencontainers.image.title=Pegasus Plus
            org.opencontainers.image.description=Games on Whales Pegasus frontend with additional emulator integrations
            org.opencontainers.image.source=https://github.com/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}

      - name: Build and publish image
        uses: docker/build-push-action@v6
        with:
          context: containers/pegasus-plus
          file: containers/pegasus-plus/Dockerfile
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
